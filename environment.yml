# 同步单一发布权重推理环境，并移除不再使用的配置解析包。
name: mam-wisequery-report
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytorch=2.1.0
  - torchvision=0.16.0
  - pytorch-cuda=11.8
  - pip:
      - numpy==1.26.4
      - peft==0.13.2
      - Pillow==12.0.0
      - PyYAML==6.0.2
      - sentencepiece==0.2.1
      - timm==1.0.19
      - transformers==4.35.2
